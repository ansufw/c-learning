# Creating modular file `C` programming

ref: https://www.youtube.com/watch?v=yA8uXpZL-QM

## Usage

1. compile the program using gcc or clang 
    ```
    gcc -Wall function1.c function2.c function3.c main.c -o m` or `clang -Wall *.c -o m
    ```
2. run the program, `./m`

